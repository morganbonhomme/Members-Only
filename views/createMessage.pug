extends layout

block content
  .backgroundmailbox
    .container.mt-5
      .row
        .col-8
          h2.text-center.mb-3.mt-5 What do you want to gossip about ?
          .honeydew-background.p-3.shadow
            form(method='POST', action='')
              .form-group
                label(for='title') Title :
                input.form-control(type='text', name='title', placeholder='Title', value=(undefined===message ? '' : message.title))
              .form-group
                label(for='text') Message :
                textarea.form-control(type='text', name='text', placeholder='Message', value=(undefined===message ? '' : message.text), rows='3')
              .text-center
                p.text-secondary.font-italic.mt-2 Only member of the club will be able to read your username.
                button.btn.red-button.text-center(type='submit') Submit
            if errors
              for error in errors
                .alert.alert-info.mt-4!= error.msg